Repo is spec-first with OpenSpec + wai artifacts; no src/ or js/ implementation yet. Best tracer-bullet path is a thin end-to-end slice that proves build + C++/Embind/TS boundary + ingest/query loop before full default seams. Active openspec changes cover future seams (neighbor graph, projector, clusterer, file store, etc.), so tracer bullet should stay below that complexity and likely focus on current v0.1 baseline.
