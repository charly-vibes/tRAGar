Architecture boundary: the tracer bullet uses TRAGar.stores.memory() plus TRAGar.embedders.custom(...) with deterministic vectors so the first slice has zero network and zero persistence dependencies. The first deliverable is a browser example that exercises the library surface rather than an app-first branch. Follow-on slices reintroduce real defaults in order: transformers.js, OPFS/IndexedDB, queryStream.
